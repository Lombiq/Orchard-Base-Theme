@* Use shape overriding to include your theme files here if you use this as the base theme. Use Model.LayoutModel to
   access the Model of the Layout.cshtml file. This view is rendered below the resources tag helper so anything injected
   by the resource manager already exists. *@
