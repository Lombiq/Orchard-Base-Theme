@* This empty file ensures, that if a derived theme doesn't override this shape it still won't try to render the default
   content which includes stuff like "<div class="widget widget-layout-injection">" which is invalid inside the <head>
   HTML element. *@
